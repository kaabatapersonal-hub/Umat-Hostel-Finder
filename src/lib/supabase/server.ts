// Server-side Supabase client — stubbed this session.
// Filled in Session 2 once we have a schema and RLS policies to talk to
// (will use `createServerClient` from @supabase/ssr with the Next.js cookie store).

export {};
